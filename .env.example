# Supabase Project Configuration (Frontend — Next.js)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Backend Python Worker (bypasses RLS — keep secret, never prefix with NEXT_PUBLIC_)
SUPABASE_SERVICE_KEY=your_supabase_service_role_key_here

# AMD Developer Cloud FastAPI backend URL (publicly reachable IP/port of your ROCm notebook)
# Used by the Next.js frontend to POST memory dumps for processing
NEXT_PUBLIC_AMD_BACKEND_URL=http://YOUR_AMD_CLOUD_INSTANCE_IP:8000

# Fireworks AI — optional Gemma 3 fallback (used only when vLLM fails or GPU is unavailable)
FIREWORKS_API_KEY=your_fireworks_api_key_here
